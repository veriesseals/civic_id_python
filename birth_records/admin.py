from django.contrib import admin
from .models import NaturalizationRecord

# Register your models here.
# ------------------------------------------

# Register the NaturalizationRecord model with the admin site.
# ----------------------------------------
admin.site.register(NaturalizationRecord)