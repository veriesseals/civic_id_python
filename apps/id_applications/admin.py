from django.contrib import admin
from .models import IDApplication

# Register your models here.
# --------------------------------------------

# Register the IDApplication model with the admin site.
# ----------------------------------------
admin.site.register(IDApplication)
